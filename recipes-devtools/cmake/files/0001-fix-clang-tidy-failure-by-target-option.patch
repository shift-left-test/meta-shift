diff --git a/Source/cmcmd.cxx b/Source/cmcmd.cxx
index 823b38f..723356f 100644
--- a/Source/cmcmd.cxx
+++ b/Source/cmcmd.cxx
@@ -362,6 +362,12 @@ int cmcmd::ExecuteCMakeCommand(std::vector<std::string>& args)
         tidy_cmd.push_back("--");
         tidy_cmd.insert(tidy_cmd.end(), orig_cmd.begin(), orig_cmd.end());

+        std::string target_sys;
+        if (cmSystemTools::GetEnv("TARGET_SYS", target_sys)) {
+          tidy_cmd.push_back("-target");
+          tidy_cmd.push_back(target_sys);
+        }
+
         // Run the tidy command line.  Capture its stdout and hide its stderr.
         std::string stdOut;
         std::string stdErr;
